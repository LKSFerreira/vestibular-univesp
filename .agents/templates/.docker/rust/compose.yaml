# =============================================================================
# Docker Compose - Rust (Otimizado com Multi-stage Cache)
# =============================================================================

services:
  app_<nome-do-projeto>_:
    build:
      context: ../
      dockerfile: .devcontainer/Dockerfile.rust
      target: dev
    ports:
      - "8080:8080"
    volumes:
      - ../:/app
      - cargo-cache:/usr/local/cargo/registry
      - target-cache:/app/target
    working_dir: /app
    stdin_open: true
    tty: true
    env_file:
      - ../.env
    environment:
      - RUST_ENV=development
      - RUST_LOG=debug
      - RUST_BACKTRACE=1

volumes:
  cargo-cache:
  target-cache:
