---
description: Workflow de Configuração de Ambiente Docker Compose
---

# Configuração de Ambiente Docker

Este workflow foi refatorado para usar a skill `ambiente_docker`.

## Ação Obrigatória

**Delegue imediatamente** a execução para a skill `ambiente_docker` (localizada em `.agents/skills/ambiente_docker/`).

Leia o `SKILL.md` da skill para realizar o setup inicial ou manutenção de containers.
